# VKB Environment Configuration
# Managed by Ansible - do not edit manually

# Database
DATABASE_URL=postgresql://{{ postgres_user }}:{{ postgres_password }}@localhost:5432/{{ postgres_db }}
VKB_DB_HOST=localhost
VKB_DB_NAME={{ postgres_db }}
VKB_DB_USER={{ postgres_user }}
VKB_DB_PASSWORD={{ postgres_password }}

# Matrix
MATRIX_HOMESERVER=https://{{ domain }}
MATRIX_USER=@valence-bot:{{ domain }}
MATRIX_PASSWORD={{ lookup('env', 'VALENCE_BOT_PASSWORD') }}

# OpenAI (for embeddings)
OPENAI_API_KEY={{ lookup('env', 'OPENAI_API_KEY') }}

# Claude calls handled via Claude Code CLI (OAuth-authenticated)
# LLM provider abstraction allows adding more providers later

# MCP Server
MCP_HOST=127.0.0.1
MCP_PORT=8765
