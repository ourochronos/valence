# Valence Environment Configuration
# Copy to .env and customize

# Database (VKB_DB_* is the primary namespace, bridge_db_env() maps to ORO_DB_*)
VKB_DB_HOST=localhost
VKB_DB_USER=valence
VKB_DB_PASSWORD=valence
VKB_DB_NAME=valence
VKB_DB_PORT=5433

# HTTP server (optional, only with --profile server)
VALENCE_PORT=8420

# OpenAI (for embeddings — optional but recommended)
# OPENAI_API_KEY=sk-...

# Auto-capture tuning (defaults are sensible)
# VALENCE_MIN_CAPTURE_CONFIDENCE=0.50
